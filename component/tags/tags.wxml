<view class="tags">
  <block wx:for="{{tags}}" wx:key="*this">
    <view class="tag" data-tag="{{item.tag}}" style="background-color: {{item.tagColor}}" bindtap='searchThisTag'>{{item.tag}}</view>
  </block>
</view>
